# Makefile for NETCONF project
#  
#   NCX agent main directory

############### SOURCE PROFILE ##############################

SUBDIR_NM=agt
SUBDIR_CPP=

############### TARGET PROFILE ##############################

TARGET=$(TBASE)/$(SUBDIR_NM)
BIN_INST=$(TBASE)/bin

#################### MAKE RULES ########################

all: agtdummy agtlib

#################### PLATFORM DEFINITIONS ############
include ../platform/platform.profile

################ DEPENDENCIES #########################
# depend rule must be included after the 'all' make rule

include ../platform/platform.profile.depend


test:

install:


agtlib: dependencies $(LBASE)/lib$(SUBDIR_NM).so


# this dummy rule keeps make from deleting the $(OBJS) as
# intermediate files
agtdummy: dependencies $(OBJS)


clean:
	rm -f $(OBJS) $(LBASE)/lib$(SUBDIR_NM).so*

superclean:
	rm -f *~  $(DEPS) dependencies $(OBJS) $(LBASE)/lib$(SUBDIR_NM).so*



# prevent the make program from choking on all the symbols
# that get generated from autogenerated make rules
.NOEXPORT:

include dependencies
