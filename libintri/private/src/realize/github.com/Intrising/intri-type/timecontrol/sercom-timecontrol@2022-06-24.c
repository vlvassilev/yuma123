// Code generated by protoc-gen-yang(*.c) DO NOT EDIT.

/*****************************************************************************************************
 * Copyright (C) 2017-2022 by Intrising
 *  - ian0113@intrising.com.tw
 * 
 * Generated by protoc-gen-yang@gen-yang
 * 
 *****************************************************************************************************/

#include <libxml/xmlstring.h>
#include "agt.h"
#include "agt_cb.h"
#include "agt_rpc.h"
#include "agt_timer.h"
#include "agt_util.h"
#include "dlq.h"
#include "ncx.h"
#include "ncx_feature.h"
#include "ncxmod.h"
#include "ncxtypes.h"
#include "procdefs.h"
#include "rpc.h"
#include "ses.h"
#include "status.h"
#include "val.h"
#include "val_util.h"
#include "xml_util.h"

#include "sercom-timecontrol@2022-06-24.h"
#include "../../../../../../../.libintrishare/libintrishare.h"

#include "../../../../../realize/github.com/Intrising/intri-type/device/sercom-device@2022-06-24.h"
#include "../../../../../realize/github.com/golang/protobuf/ptypes/empty/sercom-empty@2022-06-24.h"
#include "../../../../../realize/github.com/golang/protobuf/ptypes/timestamp/sercom-timestamp@2022-06-24.h"

#include "../../../../../genc-trans/github.com/Intrising/intri-type/device/intri-device-trans.h"
#include "../../../../../genc-trans/github.com/Intrising/intri-type/timecontrol/intri-timecontrol-trans.h"
#include "../../../../../genc-trans/github.com/golang/protobuf/ptypes/empty/intri-empty-trans.h"
#include "../../../../../genc-trans/github.com/golang/protobuf/ptypes/timestamp/intri-timestamp-trans.h"

static ncx_module_t *intri_timecontrol_mod;

static status_t intri_timecontrol_TimeControl_GetInputMonitorStatus_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_InputClockManage *in = malloc(sizeof(*in));
  struct timecontrolpb_MonitorStatus *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  /* ian: this func has no prefix Update/Set */
  res = build_to_priv_timecontrol_InputClockManage(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_GetInputMonitorStatus(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  obj_template_t *outobj = obj_find_child(
      msg->rpc_method,
      y_M_intri_timecontrol,
      "output");
  val_value_t *outval = val_new_value();
  val_init_from_template(outval, outobj);

  res = build_to_xml_timecontrol_MonitorStatus(outval, out);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }

  dlq_block_enque(&outval->v.childQ, &msg->rpc_dataQ);

  /* debug: print `val_value_t` in `msg->rpc_dataQ` */
  // for (val_value_t *val = (val_value_t *)dlq_firstEntry(&msg->rpc_dataQ);
  //      val != NULL;
  //      val = (val_value_t *)dlq_nextEntry(val)) {
  //   val_dump_value(val, 2);
  // }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_GetDPLLStatus_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_DPLLManage *in = malloc(sizeof(*in));
  struct timecontrolpb_DPLLStatus *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  /* ian: this func has no prefix Update/Set */
  res = build_to_priv_timecontrol_DPLLManage(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_GetDPLLStatus(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  obj_template_t *outobj = obj_find_child(
      msg->rpc_method,
      y_M_intri_timecontrol,
      "output");
  val_value_t *outval = val_new_value();
  val_init_from_template(outval, outobj);

  res = build_to_xml_timecontrol_DPLLStatus(outval, out);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }

  dlq_block_enque(&outval->v.childQ, &msg->rpc_dataQ);

  /* debug: print `val_value_t` in `msg->rpc_dataQ` */
  // for (val_value_t *val = (val_value_t *)dlq_firstEntry(&msg->rpc_dataQ);
  //      val != NULL;
  //      val = (val_value_t *)dlq_nextEntry(val)) {
  //   val_dump_value(val, 2);
  // }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_SetDPLLRefPriority_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_DPLLRefPriority *in = malloc(sizeof(*in));
  struct emptypb_Empty *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  /* ian: the Get func has input */
  res = build_to_priv_timecontrol_DPLLRefPriority(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_SetDPLLRefPriority(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_GetDPLLRefPriority_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_DPLLRefPriorityManage *in = malloc(sizeof(*in));
  struct timecontrolpb_DPLLRefPriority *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  /* ian: this func has no prefix Update/Set */
  res = build_to_priv_timecontrol_DPLLRefPriorityManage(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_GetDPLLRefPriority(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  obj_template_t *outobj = obj_find_child(
      msg->rpc_method,
      y_M_intri_timecontrol,
      "output");
  val_value_t *outval = val_new_value();
  val_init_from_template(outval, outobj);

  res = build_to_xml_timecontrol_DPLLRefPriority(outval, out);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }

  dlq_block_enque(&outval->v.childQ, &msg->rpc_dataQ);

  /* debug: print `val_value_t` in `msg->rpc_dataQ` */
  // for (val_value_t *val = (val_value_t *)dlq_firstEntry(&msg->rpc_dataQ);
  //      val != NULL;
  //      val = (val_value_t *)dlq_nextEntry(val)) {
  //   val_dump_value(val, 2);
  // }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_SetInputClockFreq_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_SyncEInputClockSpeed *in = malloc(sizeof(*in));
  struct emptypb_Empty *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  /* ian: the Get func has input */
  res = build_to_priv_timecontrol_SyncEInputClockSpeed(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_SetInputClockFreq(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_GetInputClockFreq_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_InputClockManage *in = malloc(sizeof(*in));
  struct timecontrolpb_SyncEInputClockSpeed *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  /* ian: this func has no prefix Update/Set */
  res = build_to_priv_timecontrol_InputClockManage(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_GetInputClockFreq(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  obj_template_t *outobj = obj_find_child(
      msg->rpc_method,
      y_M_intri_timecontrol,
      "output");
  val_value_t *outval = val_new_value();
  val_init_from_template(outval, outobj);

  res = build_to_xml_timecontrol_SyncEInputClockSpeed(outval, out);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }

  dlq_block_enque(&outval->v.childQ, &msg->rpc_dataQ);

  /* debug: print `val_value_t` in `msg->rpc_dataQ` */
  // for (val_value_t *val = (val_value_t *)dlq_firstEntry(&msg->rpc_dataQ);
  //      val != NULL;
  //      val = (val_value_t *)dlq_nextEntry(val)) {
  //   val_dump_value(val, 2);
  // }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_SetToDSource_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_ToDSource *in = malloc(sizeof(*in));
  struct emptypb_Empty *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  errstr->n = 0;
  timecontrol_TimeControl_GetToDSource(out, in, errstr);
  if (errstr->n > 0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }
  res = build_to_priv_timecontrol_ToDSource(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_SetToDSource(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_GetToDSource_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct emptypb_Empty *in = malloc(sizeof(*in));
  struct timecontrolpb_ToDSource *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  errstr->n = 0;
  timecontrol_TimeControl_GetToDSource(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  obj_template_t *outobj = obj_find_child(
      msg->rpc_method,
      y_M_intri_timecontrol,
      "output");
  val_value_t *outval = val_new_value();
  val_init_from_template(outval, outobj);

  res = build_to_xml_timecontrol_ToDSource(outval, out);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }

  dlq_block_enque(&outval->v.childQ, &msg->rpc_dataQ);

  /* debug: print `val_value_t` in `msg->rpc_dataQ` */
  // for (val_value_t *val = (val_value_t *)dlq_firstEntry(&msg->rpc_dataQ);
  //      val != NULL;
  //      val = (val_value_t *)dlq_nextEntry(val)) {
  //   val_dump_value(val, 2);
  // }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_SetToD_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_ToDTime *in = malloc(sizeof(*in));
  struct emptypb_Empty *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  /* ian: the Get func has input */
  res = build_to_priv_timecontrol_ToDTime(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_SetToD(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_GetToD_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_DPLLToDManage *in = malloc(sizeof(*in));
  struct timecontrolpb_ToDTime *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  /* ian: this func has no prefix Update/Set */
  res = build_to_priv_timecontrol_DPLLToDManage(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_GetToD(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  obj_template_t *outobj = obj_find_child(
      msg->rpc_method,
      y_M_intri_timecontrol,
      "output");
  val_value_t *outval = val_new_value();
  val_init_from_template(outval, outobj);

  res = build_to_xml_timecontrol_ToDTime(outval, out);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }

  dlq_block_enque(&outval->v.childQ, &msg->rpc_dataQ);

  /* debug: print `val_value_t` in `msg->rpc_dataQ` */
  // for (val_value_t *val = (val_value_t *)dlq_firstEntry(&msg->rpc_dataQ);
  //      val != NULL;
  //      val = (val_value_t *)dlq_nextEntry(val)) {
  //   val_dump_value(val, 2);
  // }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_SetFrequencyControl_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_DPLLFrequencyControl *in = malloc(sizeof(*in));
  struct emptypb_Empty *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  /* ian: the Get func has input */
  res = build_to_priv_timecontrol_DPLLFrequencyControl(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_SetFrequencyControl(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_GetFrequencyControl_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_DPLLManage *in = malloc(sizeof(*in));
  struct timecontrolpb_DPLLFrequencyControl *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  /* ian: this func has no prefix Update/Set */
  res = build_to_priv_timecontrol_DPLLManage(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_GetFrequencyControl(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  obj_template_t *outobj = obj_find_child(
      msg->rpc_method,
      y_M_intri_timecontrol,
      "output");
  val_value_t *outval = val_new_value();
  val_init_from_template(outval, outobj);

  res = build_to_xml_timecontrol_DPLLFrequencyControl(outval, out);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }

  dlq_block_enque(&outval->v.childQ, &msg->rpc_dataQ);

  /* debug: print `val_value_t` in `msg->rpc_dataQ` */
  // for (val_value_t *val = (val_value_t *)dlq_firstEntry(&msg->rpc_dataQ);
  //      val != NULL;
  //      val = (val_value_t *)dlq_nextEntry(val)) {
  //   val_dump_value(val, 2);
  // }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_SetPhaseControl_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_DPLLPhaseControl *in = malloc(sizeof(*in));
  struct emptypb_Empty *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  /* ian: the Get func has input */
  res = build_to_priv_timecontrol_DPLLPhaseControl(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_SetPhaseControl(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_GetPhaseControl_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_DPLLManage *in = malloc(sizeof(*in));
  struct timecontrolpb_DPLLPhaseControl *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  /* ian: this func has no prefix Update/Set */
  res = build_to_priv_timecontrol_DPLLManage(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_GetPhaseControl(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  obj_template_t *outobj = obj_find_child(
      msg->rpc_method,
      y_M_intri_timecontrol,
      "output");
  val_value_t *outval = val_new_value();
  val_init_from_template(outval, outobj);

  res = build_to_xml_timecontrol_DPLLPhaseControl(outval, out);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }

  dlq_block_enque(&outval->v.childQ, &msg->rpc_dataQ);

  /* debug: print `val_value_t` in `msg->rpc_dataQ` */
  // for (val_value_t *val = (val_value_t *)dlq_firstEntry(&msg->rpc_dataQ);
  //      val != NULL;
  //      val = (val_value_t *)dlq_nextEntry(val)) {
  //   val_dump_value(val, 2);
  // }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_SetPhaseSlopeLimit_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_DPLLPhaseSlopeLimit *in = malloc(sizeof(*in));
  struct emptypb_Empty *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  /* ian: the Get func has input */
  res = build_to_priv_timecontrol_DPLLPhaseSlopeLimit(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_SetPhaseSlopeLimit(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_GetPhaseSlopeLimit_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_DPLLManage *in = malloc(sizeof(*in));
  struct timecontrolpb_DPLLPhaseSlopeLimit *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  /* ian: this func has no prefix Update/Set */
  res = build_to_priv_timecontrol_DPLLManage(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_GetPhaseSlopeLimit(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  obj_template_t *outobj = obj_find_child(
      msg->rpc_method,
      y_M_intri_timecontrol,
      "output");
  val_value_t *outval = val_new_value();
  val_init_from_template(outval, outobj);

  res = build_to_xml_timecontrol_DPLLPhaseSlopeLimit(outval, out);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }

  dlq_block_enque(&outval->v.childQ, &msg->rpc_dataQ);

  /* debug: print `val_value_t` in `msg->rpc_dataQ` */
  // for (val_value_t *val = (val_value_t *)dlq_firstEntry(&msg->rpc_dataQ);
  //      val != NULL;
  //      val = (val_value_t *)dlq_nextEntry(val)) {
  //   val_dump_value(val, 2);
  // }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_SetPhaseControlTimer_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_DPLLPhaseControlTimer *in = malloc(sizeof(*in));
  struct emptypb_Empty *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  /* ian: the Get func has input */
  res = build_to_priv_timecontrol_DPLLPhaseControlTimer(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_SetPhaseControlTimer(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  free(in);
  free(out);
  free(errstr);
  return res;
}
static status_t intri_timecontrol_TimeControl_GetPhaseControlTimer_invoke(
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode) {
  status_t res = NO_ERR;
  struct timecontrolpb_DPLLManage *in = malloc(sizeof(*in));
  struct timecontrolpb_DPLLPhaseControlTimer *out = malloc(sizeof(*out));
  GoString *errstr = malloc(sizeof(*errstr));

  /* ian: this func has no prefix Update/Set */
  res = build_to_priv_timecontrol_DPLLManage(msg->rpc_input, in);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }
  errstr->n = 0;
  timecontrol_TimeControl_GetPhaseControlTimer(in, out, errstr);
  if (errstr->n >0) {
    const xmlChar *errorstr = NULL;
    val_value_t *errorval = NULL;
    ncx_errinfo_t *errinfo = ncx_new_errinfo();
    errinfo->error_message = (xmlChar*)(errstr->p);
    res = ERR_NCX_INVALID_VALUE;
    agt_record_error_errinfo(
        scb,
        &msg->mhdr,
        NCX_LAYER_RPC,
        res,
        methnode,
        NCX_NT_NONE,
        errorstr,
        NCX_NT_NONE,
        errorval,
        errinfo);
    log_debug("err= %s", errstr->p);
    free(in);
    free(out);
    free(errstr);
    return res;
  }

  obj_template_t *outobj = obj_find_child(
      msg->rpc_method,
      y_M_intri_timecontrol,
      "output");
  val_value_t *outval = val_new_value();
  val_init_from_template(outval, outobj);

  res = build_to_xml_timecontrol_DPLLPhaseControlTimer(outval, out);
  if (res != NO_ERR) {
    free(in);
    free(out);
    free(errstr);
    return SET_ERROR(res);
  }

  dlq_block_enque(&outval->v.childQ, &msg->rpc_dataQ);

  /* debug: print `val_value_t` in `msg->rpc_dataQ` */
  // for (val_value_t *val = (val_value_t *)dlq_firstEntry(&msg->rpc_dataQ);
  //      val != NULL;
  //      val = (val_value_t *)dlq_nextEntry(val)) {
  //   val_dump_value(val, 2);
  // }

  free(in);
  free(out);
  free(errstr);
  return res;
}

status_t y_sercom_timecontrol_init(
    const xmlChar *modname,
    const xmlChar *revision) {
  status_t res = NO_ERR;
  agt_profile_t *agt_profile = agt_get_profile();

  if (xml_strcmp(modname, y_M_intri_timecontrol)) {
    return ERR_NCX_UNKNOWN_MODULE;
  }
  if (revision && xml_strcmp(revision, y_R_intri_timecontrol)) {
    return ERR_NCX_WRONG_VERSION;
  }

  res = agt_load_sil_code(
    y_M_intri_device,
    y_R_intri_device,
    true);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_load_sil_code(
    y_M_intri_timestamp,
    y_R_intri_timestamp,
    true);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_load_sil_code(
    y_M_intri_empty,
    y_R_intri_empty,
    true);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = ncxmod_load_module(
      y_M_intri_timecontrol,
      y_R_intri_timecontrol,
      &agt_profile->agt_savedevQ,
      &intri_timecontrol_mod);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetInputMonitorStatus",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_GetInputMonitorStatus_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetDPLLStatus",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_GetDPLLStatus_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-SetDPLLRefPriority",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_SetDPLLRefPriority_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetDPLLRefPriority",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_GetDPLLRefPriority_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-SetInputClockFreq",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_SetInputClockFreq_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetInputClockFreq",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_GetInputClockFreq_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-SetToDSource",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_SetToDSource_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetToDSource",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_GetToDSource_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-SetToD",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_SetToD_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetToD",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_GetToD_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-SetFrequencyControl",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_SetFrequencyControl_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetFrequencyControl",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_GetFrequencyControl_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-SetPhaseControl",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_SetPhaseControl_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetPhaseControl",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_GetPhaseControl_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-SetPhaseSlopeLimit",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_SetPhaseSlopeLimit_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetPhaseSlopeLimit",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_GetPhaseSlopeLimit_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-SetPhaseControlTimer",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_SetPhaseControlTimer_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  res = agt_rpc_register_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetPhaseControlTimer",
      AGT_RPC_PH_INVOKE,
      intri_timecontrol_TimeControl_GetPhaseControlTimer_invoke);
  if (res != NO_ERR) {
    return SET_ERROR(res);
  }

  return res;
}

status_t y_sercom_timecontrol_init2(void) {
  status_t res = NO_ERR;
  return res;
}

void y_sercom_timecontrol_cleanup(void) {
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetInputMonitorStatus");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetDPLLStatus");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-SetDPLLRefPriority");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetDPLLRefPriority");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-SetInputClockFreq");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetInputClockFreq");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-SetToDSource");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetToDSource");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-SetToD");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetToD");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-SetFrequencyControl");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetFrequencyControl");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-SetPhaseControl");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetPhaseControl");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-SetPhaseSlopeLimit");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetPhaseSlopeLimit");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-SetPhaseControlTimer");
  agt_rpc_unregister_method(
      y_M_intri_timecontrol,
      "sercom-timecontrol-TimeControl-GetPhaseControlTimer");
}
